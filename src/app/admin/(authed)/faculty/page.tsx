import { PlaceholderView } from '../_components/mock';

export default function FacultyPage() {
  return (
    <PlaceholderView
      title="Faculty"
      breadcrumb="ACADEMIC"
      desc="Instructor roster, load balancing, industry-active hours, contract status."
    />
  );
}
