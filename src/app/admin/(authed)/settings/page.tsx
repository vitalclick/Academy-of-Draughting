import { PlaceholderView } from '../_components/mock';

export default function SettingsPage() {
  return (
    <PlaceholderView
      title="Settings"
      breadcrumb="SETTINGS"
      desc="Academy profile, intake calendar, fee structure, document requirements, integrations."
    />
  );
}
