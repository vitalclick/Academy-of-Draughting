import { PlaceholderView } from '../_components/mock';

export default function ReportsPage() {
  return (
    <PlaceholderView
      title="Reports"
      breadcrumb="FINANCE"
      desc="Enrollment funnel reports, completion rates, employer outcomes, regulatory exports (DHET, QCTO)."
    />
  );
}
