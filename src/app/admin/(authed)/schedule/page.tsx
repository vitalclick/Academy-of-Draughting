import { PlaceholderView } from '../_components/mock';

export default function SchedulePage() {
  return (
    <PlaceholderView
      title="Schedule"
      breadcrumb="ACADEMIC"
      desc="Cohort and module schedule editor — weekly grid, conflict detection, room assignments."
    />
  );
}
